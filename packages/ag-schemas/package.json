{
  "name": "@ag/schemas",
  "version": "2.0.0",
  "author": "mitrohov-dmitriy",
  "private": "true",
  "main": "dist/index.js",
  "license": "SEE LICENSE IN LICENSE.md",
  "scripts": {
    "dev": "pnpm delete-build && npx tsc --watch",
    "build": "pnpm delete-build && npx tsc",
    "lint": "eslint . --fix",
    "typecheck": "vue-tsc --noEmit",
    "delete-build": "rm -rf dist"
  },
  "peerDependencies": {
    "@nestjs/swagger": "catalog:",
    "class-transformer": "catalog:",
    "class-validator": "catalog:",
    "@ag/eslint": "workspace:*",
    "eslint": "catalog:"
  }
}
